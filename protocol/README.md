# Protocolo

Documentos maestros del investigador, nunca modificados:
- `protocolo-experimental-programa-I-v1-0.docx` — texto maestro del protocolo v1.0
- `la-intriga-version-5-2.docx` — marco conceptual v5.2

El protocolo vigente es **v1.1 = v1.0 + Enmiendas 1–6 + definición de L_U(T)/L_U(M₀)**
(nomenclatura ratificada; v1.2 reservada para post-congelamiento). Fuentes de las enmiendas:
`changelog-v1-0-a-v1-1.md` (Enmiendas 1–5), `enmienda-6.md` (trazabilidad y control de ruido
dual), `definicion-L-M0.md` (contabilidad de identificador, especificación y parámetros;
incorporada por referencia en §7).

## Estado: CONGELADO 2026-08-27

Texto congelado: **`protocolo-v1-1-consolidado.md`** y su exportación **`protocolo-v1-1-congelado.pdf`**.

| | |
|---|---|
| Fecha de congelamiento | 2026-08-27 |
| Tag | `fase-ia-v1.1` (anotado, commit `8539670`) |
| Hashes SHA-256 | `/HASHES.txt` — verificar con `sha256sum -c HASHES.txt` desde la raíz |
| Depósito OSF | **HECHO** — https://osf.io/yq9hr (27-08-2026, template Open-Ended, licencia CC BY 4.0; PDF congelado y `HASHES.txt` archivados adentro) |
| Revisión experta humana independiente | **PENDIENTE** |

**Congelar no es preregistrar** — y aquí están hechas las dos cosas. Con el depósito OSF del
27-08-2026, la v1.1 **puede citarse como pre-registro público**: el registro archiva el PDF
congelado y `HASHES.txt`, de modo que el hash citado es verificable contra el depósito.

## Congelamiento — pasos, todos cumplidos

1. ~~Exportar el protocolo v1.1 consolidado a PDF~~ — **CUMPLIDO:** `protocolo-v1-1-congelado.pdf`.
2. ~~`sha256sum` y registrar fecha + hash~~ — **CUMPLIDO:** cuatro documentos en `/HASHES.txt`, fechado 2026-08-27.
3. ~~Commit etiquetado~~ — **CUMPLIDO:** tag anotado `fase-ia-v1.1` (se usó este nombre, no
   `protocolo-v1.1-congelado`, porque el snapshot abarca el fin de la fase asistida por IA y no
   solo el protocolo).
4. ~~Preregistro público (OSF) con el mismo hash~~ — **CUMPLIDO:** https://osf.io/yq9hr,
   registrado el 2026-08-27 (template Open-Ended, licencia CC BY 4.0). El registro contiene
   `protocolo-v1-1-congelado.pdf` y `HASHES.txt`, por lo que el hash depositado es el mismo
   que verifica `sha256sum -c HASHES.txt`.

## Regla desde el congelamiento (en vigor)

Nada de lo hallado en §11.1 modifica v1.1. Se registra como desviación documentada o motiva
una futura v1.2 explícita.

Los documentos congelados **no se editan**: cualquier edición rompe `/HASHES.txt`. Las
anotaciones posteriores sobre ellos van en `/POST-FREEZE-NOTES.md`.
